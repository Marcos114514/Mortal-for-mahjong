# Application

Run `npm install` and `npm run dev`, then open the local Vite URL.

The Mahjong RL Agent demo component is `index.vue`.

What to show in the short video:

1. Start or reset a round with `New Round`.
2. Click one tile in the human hand to discard.
3. Watch the two random agents and the Mortal RL agent take turns.
4. Point out the side panel showing the RL agent's latest decision and ranked
   action values.
5. Mention that `model_link.txt` is where the external checkpoint link should be
   placed before submission, because trained models should not be included in
   the course ZIP/RAR.
